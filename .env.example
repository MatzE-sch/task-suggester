# Database
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=tasksuggester
DATABASE_URL=postgresql://taskuser:changeme@db:5432/tasksuggester

# Auth
SECRET_KEY=change-this-to-a-random-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Backend
# https://localhost + capacitor://localhost sind die WebView-Origins der Android-App
BACKEND_CORS_ORIGINS=http://localhost:3000,https://localhost,capacitor://localhost,http://localhost

# Frontend
PUBLIC_API_URL=http://localhost:8000
